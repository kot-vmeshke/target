$(document).ready(function () {
  $('.slider').slick({
    dots: true,
    slidesToShow: 3,
    // autoplay: true,
    // autoplaySpeed: 2000,
    centerMode: true,
    centerPadding: '60px',
  });
});